import {
  getInputValues,
  NumberInput,
} from './graphFactory-utils.js'

import Node from '../engine/Node.js'


// Still figuring out where to put this...
const reflowCtx = {
  utils: {
    getInputValues,
    NumberInput,
  },
  Node
}

export default reflowCtx
